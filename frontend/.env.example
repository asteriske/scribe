# Frontend Service Configuration
# Copy this file to .env and update with your settings

# ===== Service Configuration =====
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# ===== Transcriber Service =====
# URL of the transcriber service
# Examples:
#   Same machine: http://localhost:8001
#   Different machine: http://192.168.1.10:8001
#   Using hostname: http://mac-mini.local:8001
TRANSCRIBER_URL=http://localhost:8001

# Request timeout in seconds
TRANSCRIBER_TIMEOUT=300

# How often to poll for job status (seconds)
TRANSCRIBER_POLL_INTERVAL=2

# Number of retries on network failure
TRANSCRIBER_MAX_RETRIES=3

# ===== Storage Configuration =====
# Absolute path to data directory
# Update this to your actual path!
DATA_DIR=/Users/patrick/git/scribe/data

# How long to keep audio files (days)
AUDIO_CACHE_DAYS=7

# Maximum audio cache size before cleanup (GB)
MAX_AUDIO_CACHE_SIZE_GB=50

# Directory pattern for organizing transcriptions
# %Y = year, %m = month, %d = day
# Examples: "%Y/%m", "%Y", "" (flat)
TRANSCRIPTION_DIR_PATTERN=%Y/%m

# ===== Database Configuration =====
# SQLite database path
DATABASE_URL=sqlite:///./data/scribe.db

# For PostgreSQL (future):
# DATABASE_URL=postgresql://user:password@localhost/scribe

# ===== Download Configuration (yt-dlp) =====
# Audio format to download
# Options: bestaudio, worst, mp3, m4a
YTDLP_FORMAT=bestaudio

# Download timeout in seconds
YTDLP_TIMEOUT=300

# Maximum audio file size in MB (0 for unlimited)
YTDLP_MAX_FILESIZE_MB=500

# Temporary download directory
YTDLP_TEMP_DIR=/tmp/scribe_downloads

# ===== Supported Sources =====
# Comma-separated list of supported URL types
SUPPORTED_SOURCES=youtube,apple_podcasts,direct_audio

# ===== Cleanup Configuration =====
# How often to run cleanup task (hours)
CLEANUP_INTERVAL_HOURS=6

# Run cleanup when service starts
CLEANUP_ON_STARTUP=true

# ===== Logging Configuration =====
# Log file path (relative to project root)
LOG_FILE=data/logs/frontend.log

# Log format: json or text
LOG_FORMAT=text

# ===== WebSocket Configuration =====
# WebSocket ping interval (seconds)
WS_PING_INTERVAL=30

# WebSocket timeout (seconds)
WS_TIMEOUT=300

# ===== UI Configuration =====
# Application name displayed in UI
APP_NAME=Scribe

# Results per page in transcription list
RESULTS_PER_PAGE=50
